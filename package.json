{
  "name": "pmagent-mcp-server",
  "version": "1.0.0",
  "description": "PMAgent MCP Server",
  "main": "index.js",
  "scripts": {
    "start": "python -m pmagent.mcp_server",
    "dev": "nodemon --exec python -m pmagent.mcp_server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "smithery:register": "node register_smithery.js",
    "smithery:simple": "node smithery-install-direct.js",
    "smithery:server-only": "node register_server_only.js",
    "smithery:shell": "bash register_server.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
